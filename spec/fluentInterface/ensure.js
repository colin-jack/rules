describe("rules - ensure", function() {
    describe("when you use ensure that a value is an integer", function() {
        it("should fail if passed a floating point value")
        it("should fail if pass an integer")
    });
});